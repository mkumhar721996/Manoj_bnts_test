// Recognised promo codes. Only the code shown applied in the Figma design
// (FORNOROSSO10) is seeded; its 10% discount is inferred from the code name
// since no percentage value is printed anywhere in the frame.
const PROMO_CODES = {
  FORNOROSSO10: { discountPercent: 10 },
};

module.exports = { PROMO_CODES };
